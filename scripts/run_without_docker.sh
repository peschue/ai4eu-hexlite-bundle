
# this is commented out because it will only work if you have locally installed hexlite and owlapi plugin!

#python3 -m grpc_tools.protoc --python_out=. --proto_path=. --grpc_python_out=. hexlite.proto &&
#python3 server.py

